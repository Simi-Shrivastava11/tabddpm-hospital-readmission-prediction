# ── Core scientific stack ──────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11

# ── Machine learning ───────────────────────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
imbalanced-learn>=0.11        # SMOTE

# ── Deep learning (TabDDPM) ────────────────────────────────────────────────────
torch>=2.1
# Install the matching torchvision/torchaudio only if needed for your GPU:
# torchvision>=0.16
# torchaudio>=2.1

# ── Synthetic data baselines ───────────────────────────────────────────────────
ctgan>=0.5                    # CTGAN baseline
sdv>=1.9                      # SDV suite (wraps CTGAN, TVAE, etc.)

# ── Visualisation ──────────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18

# ── Dimensionality reduction / evaluation ─────────────────────────────────────
scikit-learn>=1.3             # t-SNE, PCA already included above
umap-learn>=0.5               # optional UMAP plots

# ── Statistical evaluation ─────────────────────────────────────────────────────
scipy>=1.14                   # Jensen-Shannon divergence (scipy.spatial.distance.jensenshannon)

# ── Notebook environment ───────────────────────────────────────────────────────
jupyter>=1.0
jupyterlab>=4.0
ipykernel>=6.0
ipywidgets>=8.0

# ── Experiment tracking & reproducibility ─────────────────────────────────────
tqdm>=4.66                    # progress bars during training/generation
joblib>=1.3                   # model serialisation / parallel seeds

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0            # load .env for any local config
pyyaml>=6.0                   # config files
